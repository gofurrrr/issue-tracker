1️⃣ What is the difference between var, let, and const?
ans: var is variable it can be redeclared and reassigned , 
    let can be reassigned but cant be redeclared and const is can't be reassigned and can't be redeclared.

2️⃣ What is the spread operator (...)?
ans: it used to spread elements of of an array or object , It helps to pass values easily.

3️⃣ What is the difference between map(), filter(), and forEach()?
ans: map() creates a new array by applying a function to each element ,
    filter() creates a new array with elements that pass a conditions and forEach() run the function for each element but do not return any new array.

4️⃣ What is an arrow function?
ans: it is a modern way to write functions in javascript and it is easy  to code less code needed.

5️⃣ What are template literals?
ans: its a way to write html code or strings in javascript by using      bacticks`` .